{
    "name": "oxonai-suite",
    "version": "1.0.0",
    "description": "OxonAI - Unified Development Environment",
    "scripts": {
        "cleanup": "lsof -ti:5173,3000 | xargs kill -9 2>/dev/null || true",
        "check:mongo": "echo '🔍 Checking MongoDB Atlas connection...' && cd OxonAI && node -e \"require('dotenv').config();const {MongoClient}=require('mongodb');const uri=process.env.DATABASE_URL;if(!uri){console.log('❌ DATABASE_URL not set in .env');process.exit(1)}const client=new MongoClient(uri,{serverSelectionTimeoutMS:5000});client.connect().then(()=>{console.log('✅ MongoDB Atlas Connected');client.close()}).catch(e=>{console.log('❌ MongoDB connection failed:',e.message);console.log('⚠️  Google OAuth will not work without database connection');process.exit(0)})\" || echo '⚠️  MongoDB driver missing'",
        "predev": "npm run cleanup && npm run check:mongo",
        "dev:web": "cd OxonAI && npm run dev",
        "dev:ide": "cd IDE && npm run dev",
        "dev": "concurrently \"npm run dev:web\" \"npm run dev:ide\"",
        "install:all": "npm install && concurrently \"cd OxonAI && npm install\" \"cd IDE && npm install\""
    },
    "dependencies": {
        "concurrently": "^8.2.2"
    }
}